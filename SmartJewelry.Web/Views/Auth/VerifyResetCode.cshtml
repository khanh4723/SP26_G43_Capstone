@model VerifyResetCodeViewModel
@{
    ViewData["Title"] = "Xác nhận mã";
    Layout = null;
}

<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Smart Jewelry</title>
    <link rel="stylesheet" href="~/css/bootstrap.min.css" asp-append-version="true">
    <link rel="stylesheet" href="~/css/style.css" asp-append-version="true">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@300;400;500;600;700&family=Montserrat:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-gold: #704204;
            --light-bg: #F6F5F2;
            --dark-text: #0d0d0d;
        }

        body {
            font-family: 'Montserrat', sans-serif;
            background-color: var(--light-bg);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .auth-container {
            max-width: 600px;
            width: 100%;
            margin: 2rem auto;
            background: white;
            border-radius: 16px;
            overflow: hidden;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.08);
            padding: 4rem;
        }

        .auth-title {
            font-family: 'Cormorant Garamond', serif;
            font-size: 2rem;
            font-weight: 600;
            color: var(--dark-text);
            margin-bottom: 0.5rem;
        }

        .auth-subtitle {
            color: #666;
            margin-bottom: 2rem;
        }

        .form-control {
            border: 2px solid #e0e0e0;
            border-radius: 8px;
            padding: 0.75rem 1rem;
            font-size: 0.95rem;
            transition: all 0.3s;
            text-align: center;
            font-size: 1.5rem;
            letter-spacing: 0.5rem;
        }

        .form-control:focus {
            border-color: var(--primary-gold);
            box-shadow: 0 0 0 0.2rem rgba(112, 66, 4, 0.1);
        }

        .form-label {
            font-weight: 500;
            color: var(--dark-text);
            margin-bottom: 0.5rem;
        }

        .btn-primary {
            background-color: var(--primary-gold);
            border: none;
            border-radius: 8px;
            padding: 0.75rem 2rem;
            font-weight: 600;
            transition: all 0.3s;
        }

        .btn-primary:hover {
            background-color: #8B5A0A;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(112, 66, 4, 0.3);
        }

        .alert {
            border-radius: 8px;
            border: none;
        }

        .text-link {
            color: var(--primary-gold);
            text-decoration: none;
            font-weight: 500;
        }

        .text-link:hover {
            color: #8B5A0A;
            text-decoration: underline;
        }

        .icon-wrapper {
            width: 80px;
            height: 80px;
            background: linear-gradient(135deg, var(--primary-gold) 0%, #8B5A0A 100%);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 2rem;
            font-size: 2rem;
        }
    </style>
</head>
<body>
    <div class="auth-container">
        <div class="text-end mb-3">
            <a asp-controller="Auth" asp-action="Login" class="text-link">← Quay lại đăng nhập</a>
        </div>

        <div class="icon-wrapper text-white">
            📧
        </div>

        <h2 class="auth-title text-center">Xác nhận mã</h2>
        <p class="auth-subtitle text-center">Nhập mã 6 chữ số đã được gửi đến email <strong>@Model.Email</strong></p>

        @if (ViewData["ErrorMessage"] != null)
        {
            <div class="alert alert-danger">
                @ViewData["ErrorMessage"]
            </div>
        }

        <form method="post" asp-controller="Auth" asp-action="VerifyResetCode">
            <input type="hidden" asp-for="Email" />
            <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

            <div class="mb-4">
                <label asp-for="ResetCode" class="form-label text-center d-block"></label>
                <input asp-for="ResetCode" class="form-control" placeholder="000000" maxlength="6" autofocus />
                <span asp-validation-for="ResetCode" class="text-danger small"></span>
            </div>

            <button type="submit" class="btn btn-primary w-100 mb-3">Xác nhận</button>

            <div class="text-center">
                <span class="text-muted">Không nhận được mã? </span>
                <a asp-controller="Auth" asp-action="ForgotPassword" class="text-link">Gửi lại</a>
            </div>
        </form>
    </div>

    <script src="~/js/jquery-1.11.0.min.js"></script>
    <script src="~/js/bootstrap.bundle.min.js"></script>
</body>
</html>
